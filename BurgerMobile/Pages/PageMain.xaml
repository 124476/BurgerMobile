<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BurgerMobile.Pages.PageMain"
             Title="ВИЛКА И НОЖ"
             Loaded="ContentPage_Loaded">
    <VerticalStackLayout>
        <Label 
            Text="Главная страница" FontSize="19"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackLayout Grid.Column="0">
                <Label Text="{Binding Title}" FontSize="17"></Label>
                <Label Text="{Binding Description}" FontSize="17"></Label>
            </StackLayout>
            <Image Source="{Binding BPhoto}" Grid.Column="1" WidthRequest="200"></Image>
        </Grid>
    </VerticalStackLayout>
</ContentPage>